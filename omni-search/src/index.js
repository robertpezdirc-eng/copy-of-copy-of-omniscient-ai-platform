import React from 'react';
import ReactDOM from 'react-dom/client';
import OmniDashboard from './OmniDashboard.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <OmniDashboard />
  </React.StrictMode>
);