# DISABLED on 2025-11-02: Cloud Build Gateway simplified config disabled; kept for reference only.
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/gateway:$SHORT_SHA', '-f', 'Dockerfile', '.']
    dir: 'gateway'

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/gateway:$SHORT_SHA']

images:
  - 'gcr.io/$PROJECT_ID/gateway:$SHORT_SHA'

timeout: 600s
