# Reduce Cloud Build context size; keep only files needed for minimal backend build
# Cloud Build uses .gcloudignore; if absent, it falls back to .gitignore.

# Exclude VCS
.git/
.gitignore

# Python caches
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/*.egg-info/

# Node and frontend artifacts
node_modules/
frontend/node_modules/
frontend/dist/

# Large or non-essential project areas
.venv/
venv/
ai-worker/**
backup-service/**
dashboards/**
grafana/**
ingestion/**
learning/**
scripts/**
google-cloud-sdk/**

# Exclude frontend and gateway (they have separate builds)
frontend/**
gateway/**
