{
  "name": "web-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Static web dashboard for Omni Enterprise Ultra Max",
  "scripts": {
    "build": "echo 'Static site — no build step'",
    "start": "npx serve -s ."
  }
}
